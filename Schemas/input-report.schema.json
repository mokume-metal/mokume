{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mokume.org/schemas/input-report.schema.json",
  "title": "The input reply",
  "description": "What was done with the input that was sent. This alone tells apart the case of sending something that had no effect.",
  "type": "object",
  "required": [
    "schemaVersion",
    "id",
    "accepted",
    "ignored",
    "dropped"
  ],
  "additionalProperties": false,
  "properties": {
    "schemaVersion": {
      "const": 1
    },
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "The identifier of the request this answers."
    },
    "accepted": {
      "type": "integer",
      "minimum": 0,
      "description": "How many were taken."
    },
    "ignored": {
      "type": "integer",
      "minimum": 0,
      "description": "How many were dropped, for an unknown type or the like."
    },
    "dropped": {
      "type": "integer",
      "minimum": 0,
      "description": "How many were dropped because they could not be held (a running total since launch). If this grows, too much is being sent."
    }
  }
}
