# セキュリティ方針

脆弱性の報告を歓迎する。**報告はどの言語で書いてもよい** (メンテナが日本語で作業しているだけで、リポジトリとして言語は強制しない — [ADR-0001](docs/decisions/0001-founding-principles.md) 原則 10)。

## サポートするバージョン

1.0 までは 0.x として破壊的変更を許容する ([ADR-0001](docs/decisions/0001-founding-principles.md) 原則 6)。セキュリティ修正は**最新のリリースにのみ**入れ、それより前のリリースへ backport しない。ライブラリ・CLI・プロジェクトテンプレートは 1 つのバージョンタグで同時に出荷されるため、サポート対象も 1 つに揃う。

1.0 以降の方針 (最新メジャーへの backport を行うか) は、そのときに本ファイルを改めて決め直す。

## 脆弱性の報告

**公開の Issue には書かないでほしい。** 公開の場に書かれた時点で、直す前に誰でも読める状態になるため。

代わりに GitHub の private vulnerability reporting を使う:

1. このリポジトリの [Security タブ](https://github.com/mokume-metal/mokume/security)を開く
2. **"Report a vulnerability"** から非公開の advisory を作る

これにより、公開前に報告者とメンテナだけで内容を確認し、修正を用意できる。受領の連絡と、その後の状況は同じ advisory 上で返す。

報告に含めてほしいもの (分かる範囲でよい):

- 再現の手順、または影響を確認したコード
- 影響の範囲 (何ができてしまうか)
- 動作環境 (macOS のバージョン・チップ・mokume のバージョンまたはコミット)
