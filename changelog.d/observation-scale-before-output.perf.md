<!--
SPDX-FileCopyrightText: 2026 mokume-metal
SPDX-License-Identifier: MIT
-->

**`scale` を指定した観測が、指定した画素数ぶんの費用で済むようになりました。** これまで縮小は出力段を通した**後**に効いていたため、`scale: 0.5` を頼んでも実寸の全画素を変換してから 4 分の 3 を捨てていました。間引きを出力段の前へ移したので、費用が `scale²` に比例して落ちます — 出力段は画素ごとの純関数で間引きは元の点をそのまま拾うので、**出る絵は 1 ビットも変わりません**。
